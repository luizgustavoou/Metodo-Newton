#include <stdio.h>
#include <math.h>

//
//
//

int main() {
    int valor_anterior;
    int valor_inicial=2;
    double limite = pow(10, -4);
    double erro;

    while (1) {
        valor_anterior = valor_inicial;
        //valor_inicial = valor_inicial - f(x) / f'(x);

        /* erro = abs(valor_inicial-valor_anterior) / valor_inicial;
        if(erro < limite) break; */
    }

    return 0;
}
